import React from 'react';
import ClientLogin from './ClientLogin';

export const metadata = {
  title: "Login - Admin",
  description: "",
};


const Page = () => {
    return (
        <ClientLogin/>
    );
}

export default Page;
